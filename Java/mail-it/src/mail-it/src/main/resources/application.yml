spring:
  mail:
    host: ${EMAIL_HOST:mailhog}
    port: ${EMAIL_PORT:8025}

management:
  endpoint:
    health:
      show-details: always

mail-auth:
  user: ${BASIC_AUTH_USER}
  password: ${BASIC_AUTH}

logging:
  level:
    root: INFO
