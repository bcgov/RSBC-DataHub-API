spring:
  mail:
    host: ${EMAIL_HOST:mailhog}
    port: ${EMAIL_PORT:8025}

management:
  endpoint:
    health:
      show-details: always

mail-auth:
  user: ${EMAIL_BASIC_AUTH_USER}
  password: ${EMAIL_BASIC_AUTH}

logging:
  level:
    root: INFO
